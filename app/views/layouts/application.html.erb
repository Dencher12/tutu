<!DOCTYPE html>
<html>
  <head>
    <title>Tutu</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <p id="notice" class="notice"><%= notice %></p>
    <p id="alert" class="alert"><%= alert %></p>

    <% if user_signed_in? %>
      <%= "Hi, #{current_user.name}!" %><br>
      <%= link_to 'Sign out', destroy_user_session_path, method: :delete  %>
    <% else %>
      <%= link_to 'Sign in', new_user_session_path %>
    <% end %>      

    <%= yield %>

    <footer>
      (с) 2019 Денис Червяков. Учебный проект в 
      <a href='http://www.thinknetica.com' target="_blank">
        онлайн-школе Thinknetica
      </a>
    </footer>

    <style>
      footer {
        position: absolute;
        left: 32px;
        bottom: 20px;
      }
    </style>
  </body>
</html>
